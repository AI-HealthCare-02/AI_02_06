  ---
  자동화된 보안 (현재 적용됨)

  미들웨어 자동 적용 (app/main.py)

  app.add_middleware(SecurityMiddleware)  # 보안 헤더 + 패턴 차단
  app.add_middleware(CORSMiddleware, ...)  # CORS 제어

  Pre-commit Hook

  - Ruff 린트/포맷만 적용
  - 보안 검사 도구(Bandit, Semgrep) 미적용

  ---
  코드 추가 시 필수 체크리스트

  1. 새 엔드포인트 작성 시

  from app.dependencies.security import get_current_account

  @router.post("/")
  async def create_something(
      data: SomeDTO,
      current_account: Annotated[Account, Depends(get_current_account)],  #
  필수!
      service: SomeServiceDep,
  ):
      # current_account.id로 소유권 검증
      ...

  2. 사용자 입력 받을 때

  from app.validators.secure_types import CleanString, SafeString

  class SomeDTO(BaseModel):
      name: CleanString  # HTML sanitize + 패턴 검사
      description: SafeString  # 패턴 검사만

  3. DB 쿼리 시

  # 반드시 ORM 사용 (직접 SQL 금지)
  await Model.filter(field=value)  # 안전
  await Model.get_or_none(id=user_input)  # 안전

  # 금지
  await conn.execute(f"SELECT * FROM table WHERE id = {user_input}")

  4. 외부 데이터 반환 시

  # 민감 정보 필터링
  class ResponseDTO(BaseModel):
      id: UUID
      name: str
      # password, token 등 절대 포함 금지

  5. Rate Limiting 필요 시

  from app.dependencies.rate_limiter import get_rate_limiter

  @router.post("/sensitive-action")
  async def sensitive_action(
      rate_limiter: Annotated[RateLimiter, Depends(get_rate_limiter)],
      request: Request,
  ):
      rate_limiter.check(request.client.host)
      ...
  
  app/
  ├── dependencies/
  │   └── security.py          # get_current_account (인증 의존성)
  ├── middlewares/
  │   └── security.py          # 보안 헤더 + 공격 패턴 차단
  ├── validators/
  │   ├── secure_types.py      # SafeString, CleanString
  │   └── common.py            # 공통 검증
  ├── services/
  │   ├── oauth.py             # OAuth + RTR
  │   └── rate_limiter.py      # Rate Limiting
  ├── utils/
  │   ├── security.py          # bcrypt 해싱
  │   └── jwt/
  │       ├── backends.py      # JWT 인코딩/디코딩
  │       └── tokens.py        # Token 클래스
  └── core/
      └── config.py            # 환경변수 관리